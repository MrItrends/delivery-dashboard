import { AppShell } from '@/components/layout/AppShell'
import { PriorityAreaWorkspace } from '@/components/priority-area/PriorityAreaWorkspace'

export default function PriorityAreasPage() {
  return (
    <AppShell>
      <PriorityAreaWorkspace />
    </AppShell>
  )
}
